#!/bin/bash

// This includes all functions required even if not used
// git status
git add --all
git commit -m "Remove volume fraction from convergence check and improve flow resistance increase method"
git push -u origin main
